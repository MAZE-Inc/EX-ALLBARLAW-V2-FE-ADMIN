# DG08. 공통 코드 정의서

## 1. 개요

본 문서는 EX-ALLBARLAW-V2-FE-ADMIN 프로젝트에서 사용하는 공통 코드 및 상수를 정의합니다.

---

## 2. 라우터 상수 (routePath.ts)

### 2.1 인증 라우트
```typescript
export const ROUTE_PATH = {
  LOGIN: '/login',
}
```

### 2.2 관리 라우트
```typescript
export const ROUTE_PATH = {
  // 관리자 관리
  ADMIN_MANAGEMENT: '/admin',
  ADMIN_REGISTER: 'register',

  // 카테고리 관리
  CATEGORY_MANAGEMENT: '/category',

  // 변호사 관리
  LAWYER_MANAGEMENT: '/lawyer',
  LAWYER_DETAIL: ':lawyerId',
  LAWYER_EDIT: ':lawyerId/edit',

  // 회원 관리
  MEMBER: '/member',

  // 변호사 회원
  LAWYER_MEMBER: '/lawyer-member',
}
```

### 2.3 컨텐츠 라우트
```typescript
export const ROUTE_PATH = {
  // 블로그
  CONTENT_BLOG: '/content/blog',

  // 영상
  CONTENT_VIDEO: '/content/video',

  // 법률지식
  CONTENT_KNOWLEDGE: '/content/knowledge',
}
```

### 2.4 게시판 라우트
```typescript
export const ROUTE_PATH = {
  // 공지사항
  BOARD_NOTICE: '/board/notice',
  BOARD_NOTICE_EDIT: 'edit',

  // FAQ
  BOARD_FAQ: '/board/faq',
  BOARD_FAQ_EDIT: 'edit',

  // 법률용어사전
  BOARD_LEGAL_DICTIONARY: '/board/legal-dictionary',
  BOARD_LEGAL_DICTIONARY_EDIT: 'edit',
}
```

### 2.5 광고 라우트
```typescript
export const ROUTE_PATH = {
  // 배너
  AD_BANNER: '/ad/banner',
  AD_BANNER_CATEGORY: 'category-banner',
  AD_BANNER_SUB_CATEGORY: 'sub-category-banner',

  // 로펌 광고
  AD_LAWFIRM: '/ad/lawfirm',
  AD_LAWFIRM_CREATE: '/ad/lawfirm/create',

  // 변호사 광고
  AD_LAWYER: '/ad/lawyer',
  AD_LAWYER_CREATE: '/ad/lawyer/create',
}
```

### 2.6 기타 라우트
```typescript
export const ROUTE_PATH = {
  // 채팅
  CHAT_LIST: '/chat-list',

  // 통계
  STATISTICS_LIST: '/statistics',
}
```

---

## 3. API 엔드포인트 상수

### 3.1 인증 API
```typescript
export const AUTH_API = {
  LOGIN: '/admin/auth/login',
  LOGOUT: '/admin/auth/logout',
  REFRESH: '/admin/auth/refresh',
}
```

### 3.2 관리자 API
```typescript
export const ADMIN_API = {
  LIST: '/admin/admins',
  CREATE: '/admin/admins',
  UPDATE: '/admin/admins/:id',
  DELETE: '/admin/admins/:id',
}
```

### 3.3 회원 API
```typescript
export const MEMBER_API = {
  LIST: '/admin/members',
  DETAIL: '/admin/members/:id',
  UPDATE: '/admin/members/:id',
}
```

### 3.4 변호사 API
```typescript
export const LAWYER_API = {
  LIST: '/admin/lawyers',
  DETAIL: '/admin/lawyers/:id',
  UPDATE: '/admin/lawyers/:id',
  APPROVE: '/admin/lawyers/:id/approve',
  REJECT: '/admin/lawyers/:id/reject',
}
```

### 3.5 컨텐츠 API
```typescript
export const CONTENT_API = {
  BLOG_LIST: '/admin/blog-cases',
  BLOG_DETAIL: '/admin/blog-cases/:id',
  VIDEO_LIST: '/admin/video-cases',
  VIDEO_DETAIL: '/admin/video-cases/:id',
  KNOWLEDGE_LIST: '/admin/knowledge',
}
```

### 3.6 게시판 API
```typescript
export const BOARD_API = {
  NOTICE_LIST: '/admin/notices',
  FAQ_LIST: '/admin/faqs',
  LEGAL_TERM_LIST: '/admin/legal-terms',
}
```

---

## 4. 상태 코드

### 4.1 변호사 승인 상태
```typescript
export const LAWYER_STATUS = {
  PENDING: 'pending',      // 승인 대기
  APPROVED: 'approved',    // 승인됨
  REJECTED: 'rejected',    // 반려됨
  SUSPENDED: 'suspended',  // 정지됨
} as const
```

### 4.2 회원 상태
```typescript
export const MEMBER_STATUS = {
  ACTIVE: 'active',        // 활성
  INACTIVE: 'inactive',    // 비활성
  WITHDRAWN: 'withdrawn',  // 탈퇴
} as const
```

### 4.3 컨텐츠 상태
```typescript
export const CONTENT_STATUS = {
  DRAFT: 'draft',          // 임시저장
  PUBLISHED: 'published',  // 게시됨
  HIDDEN: 'hidden',        // 숨김
  DELETED: 'deleted',      // 삭제됨
} as const
```

### 4.4 배너 상태
```typescript
export const BANNER_STATUS = {
  ACTIVE: 'active',        // 노출 중
  INACTIVE: 'inactive',    // 비노출
  SCHEDULED: 'scheduled',  // 예약됨
  EXPIRED: 'expired',      // 만료됨
} as const
```

---

## 5. Ant Design 테이블 설정

### 5.1 페이지 사이즈 옵션
```typescript
export const PAGE_SIZE_OPTIONS = ['10', '20', '50', '100']
export const DEFAULT_PAGE_SIZE = 10
```

### 5.2 테이블 스크롤 설정
```typescript
export const TABLE_SCROLL = {
  x: 1200,
  y: 'calc(100vh - 300px)',
}
```

---

## 6. 날짜 포맷

```typescript
export const DATE_FORMAT = {
  DATE: 'YYYY-MM-DD',
  DATETIME: 'YYYY-MM-DD HH:mm:ss',
  TIME: 'HH:mm:ss',
  DISPLAY_DATE: 'YYYY년 MM월 DD일',
  DISPLAY_DATETIME: 'YYYY년 MM월 DD일 HH:mm',
}
```

---

## 7. 파일 업로드 설정

```typescript
export const UPLOAD_CONFIG = {
  MAX_FILE_SIZE: 10 * 1024 * 1024, // 10MB
  ACCEPTED_IMAGE_TYPES: ['image/jpeg', 'image/png', 'image/gif', 'image/webp'],
  ACCEPTED_VIDEO_TYPES: ['video/mp4', 'video/webm'],
}
```

---

## 8. 에러 메시지

### 8.1 인증 에러
```typescript
export const AUTH_ERROR_MESSAGE = {
  INVALID_CREDENTIALS: '아이디 또는 비밀번호가 올바르지 않습니다.',
  SESSION_EXPIRED: '세션이 만료되었습니다. 다시 로그인해주세요.',
  UNAUTHORIZED: '접근 권한이 없습니다.',
}
```

### 8.2 폼 에러
```typescript
export const FORM_ERROR_MESSAGE = {
  REQUIRED: '필수 입력 항목입니다.',
  INVALID_EMAIL: '올바른 이메일 형식이 아닙니다.',
  MIN_LENGTH: (min: number) => `최소 ${min}자 이상 입력해주세요.`,
  MAX_LENGTH: (max: number) => `최대 ${max}자까지 입력 가능합니다.`,
}
```

---

## 9. 사이드바 메뉴 구조

```typescript
export const SIDEBAR_MENU = [
  {
    key: 'admin',
    icon: 'UserOutlined',
    label: '관리자 관리',
    path: '/admin',
  },
  {
    key: 'category',
    icon: 'AppstoreOutlined',
    label: '카테고리 관리',
    path: '/category',
  },
  {
    key: 'lawyer',
    icon: 'TeamOutlined',
    label: '변호사 관리',
    path: '/lawyer',
  },
  {
    key: 'member',
    icon: 'UserOutlined',
    label: '회원 관리',
    path: '/member',
  },
  {
    key: 'content',
    icon: 'FileTextOutlined',
    label: '컨텐츠 관리',
    children: [
      { key: 'blog', label: '블로그', path: '/content/blog' },
      { key: 'video', label: '영상', path: '/content/video' },
      { key: 'knowledge', label: '법률지식', path: '/content/knowledge' },
    ],
  },
  {
    key: 'board',
    icon: 'ProfileOutlined',
    label: '게시판 관리',
    children: [
      { key: 'notice', label: '공지사항', path: '/board/notice' },
      { key: 'faq', label: 'FAQ', path: '/board/faq' },
      { key: 'legal-dictionary', label: '법률용어사전', path: '/board/legal-dictionary' },
    ],
  },
  {
    key: 'ad',
    icon: 'PictureOutlined',
    label: '광고 관리',
    children: [
      { key: 'banner', label: '배너', path: '/ad/banner' },
      { key: 'lawfirm', label: '로펌 광고', path: '/ad/lawfirm' },
      { key: 'lawyer-ad', label: '변호사 광고', path: '/ad/lawyer' },
    ],
  },
  {
    key: 'chat',
    icon: 'MessageOutlined',
    label: '채팅 관리',
    path: '/chat-list',
  },
  {
    key: 'statistics',
    icon: 'BarChartOutlined',
    label: '통계',
    path: '/statistics',
  },
]
```

---

## 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| 1.0 | 2024-12-24 | - | 최초 작성 |
